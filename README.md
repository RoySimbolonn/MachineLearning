# Penerapan Algoritma K-Nearest Neighbors (KNN) untuk Sistem Rekomendasi Lagu Berbasis Content-Based Recommendation

Sistem rekomendasi lagu yang menggunakan algoritma K-Nearest Neighbors (KNN) untuk memberikan rekomendasi lagu berdasarkan kesamaan konten (content-based filtering).

## 📋 Deskripsi

Aplikasi ini mengimplementasikan algoritma KNN untuk merekomendasikan lagu-lagu yang mirip berdasarkan fitur-fitur audio dan metadata lagu. Sistem ini menggunakan pendekatan content-based recommendation yang menganalisis karakteristik lagu untuk menemukan lagu-lagu serupa.

## 🚀 Instalasi

### 1. Clone Repository

```bash
git clone https://github.com/RoySimbolonn/MachineLearning.git
cd MachineLearning
```

### 2. Install Dependencies

**Untuk sistem Anda (menggunakan python -m pip):**
```bash
python -m pip install -r requirements.txt
```

**Instalasi umum (alternatif):**
```bash
pip install -r requirements.txt
```


## 🎯 Cara Menjalankan Aplikasi

**Untuk sistem Anda:**
```bash
streamlit run app.py
```

**Cara umum (alternatif):**
```bash
python -m streamlit run app.py
```

Setelah dijalankan, aplikasi akan otomatis membuka browser pada alamat:
```
http://localhost:8501
```

## 📦 Requirements

Pastikan Anda memiliki Python 3.7 atau lebih tinggi. Dependencies utama meliputi:

- streamlit
- pandas
- numpy
- scikit-learn
- plotly

##  Teknologi yang Digunakan

- **Python**: Bahasa pemrograman utama
- **Streamlit**: Framework untuk membuat web app
- **Scikit-learn**: Library untuk implementasi algoritma KNN
- **Pandas**: Manipulasi dan analisis data
- **NumPy**: Komputasi numerik

##  Fitur

- ✅ Rekomendasi lagu berbasis kesamaan konten
- ✅ Algoritma K-Nearest Neighbors (KNN)
- ✅ Interface web interaktif dengan Streamlit
- ✅ Visualisasi data dan hasil rekomendasi

## 👨‍💻 Author

**Roy Simbolon**
- GitHub: [@RoySimbolonn](https://github.com/RoySimbolonn)

##  Lisensi

Project ini dibuat untuk keperluan pembelajaran dan pengembangan sistem rekomendasi menggunakan machine learning.

---

**Note:** Pastikan semua dependencies sudah terinstall dengan benar sebelum menjalankan aplikasi.